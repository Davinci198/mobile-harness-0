package com.jarves.mh.tools.calc

/** Shared limits for MH calculate and sleep tools. */
object CalcToolLimits {
    const val MAX_EXPRESSION_CHARS: Int = 1024
    const val DEFAULT_SLEEP_MS: Long = 1_000
    const val MIN_SLEEP_MS: Long = 0
    const val MAX_SLEEP_MS: Long = 10_000
}
